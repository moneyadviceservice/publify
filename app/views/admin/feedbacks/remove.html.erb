<%- content_for :page_heading do %>
  <i class="icon-large"></i>
  <h2 class="page-title"><%= t('.destroy_confirmation') %></h2>
<% end %>

<div class='well'>
  <p>
    <%= t(".are_you_sure")%>
  </p>

  <%= form_tag admin_feedback_path(@comment), method: :delete do %>
    <hr>
    <div class='form-actions'>
      <%= t(".action_or_other", first_action: link_to(t(".cancel"), admin_feedbacks_path(article_id: @comment.article)), second_action: submit_tag(t(".delete"), class: "btn btn-danger")) %>
    </div>
  <% end %>
</div>
